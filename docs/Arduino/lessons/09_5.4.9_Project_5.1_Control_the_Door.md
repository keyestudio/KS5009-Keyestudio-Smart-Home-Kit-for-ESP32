### 5.4.9 プロジェクト 5.1 ドアの制御


#### **1. 説明**

自動ドアや自動窓は電動装置が必要で、180度のサーボといくつかのセンサーを組み合わせることでより自動化できます。雨滴センサーを追加すれば、雨が降ったときに自動で窓を閉める動作を実現できます。RFIDを追加すれば、かざしてドアを開けるといった動作も実現できます。


#### **2. コンポーネントの知識**

**サーボ：** サーボは、ハウジング、基板、コアレスモーター、ギア、位置検出器で構成される位置制御用の駆動装置です。

その動作原理は、サーボがMCUや受信機から送られる信号を受け取り、周期20ms、幅1.5msの基準信号を生成し、得られたDCバイアス電圧とポテンショメータの電圧を比較して電圧差を出力することにあります。

基板上のICは回転方向を判断し、コアレスモーターを駆動して回転を開始します。出力は減速ギアを介してスイングアームに伝達され、位置検出器によって位置情報がフィードバックされ、所定の位置に到達したかを判断します。このため、角度変化を一定に保ち維持する必要がある制御システムに適しています。

モーター速度が一定になると、ポテンショメータは段減速ギアを介して回転し、これにより電圧差が0となり、モーターは回転を停止します。一般に、サーボの回転角度範囲は0°～180°です。

制御サーボのパルス周期は20ms、パルス幅は0.5ms～2.5msで、対応する位置は-90°～+90°です。以下は180°サーボの例です：

![図44](../media/708316fde05c62113a3024e0efb0c237.jpeg)

一般的に、サーボには茶色、赤、オレンジの3本の線があります。茶色がGND、赤が正電源線、オレンジが信号線です。

![図45](../media/35084ae289a08e35bdb8c89ceb134ba4.png)

![図46](../media/6cbf6f177ea204f7632b872497fde010.png)


#### **3. ピン**

| 窓のサーボ | 5 |
| --- | --- |
| ドアのサーボ | 13 |


#### **4. テストコード**

```c
#include <ESP32Servo.h>
Servo myservo;  // create servo object to control a servo
                // 16 servo objects can be created on the ESP32

int pos = 0;    // variable to store the servo position
// Recommended PWM GPIO pins on the ESP32 include 2,4,12-19,21-23,25-27,32-33
int servoPin = 13;

void setup() {
    // Allow allocation of all timers
    ESP32PWM::allocateTimer(0);
    ESP32PWM::allocateTimer(1);
    ESP32PWM::allocateTimer(2);
    ESP32PWM::allocateTimer(3);
    myservo.setPeriodHertz(50);    // standard 50 hz servo
    myservo.attach(servoPin, 1000, 2000); // attaches the servo on pin 18 to the servo object
    // using default min/max of 1000us and 2000us
    // different servos may require different min/max settings
    // for an accurate 0 to 180 sweep

}

void loop() {
    for (pos = 0; pos <= 180; pos += 1) { // goes from 0 degrees to 180 degrees
        // in steps of 1 degree
        myservo.write(pos);    // tell servo to go to position in variable 'pos'
        delay(15);             // waits 15ms for the servo to reach the position
    }
    for (pos = 180; pos >= 0; pos -= 1) { // goes from 180 degrees to 0 degrees
        myservo.write(pos);    // tell servo to go to position in variable 'pos'
        delay(15);             // waits 15ms for the servo to reach the position
    }

}
```

#### **5. テスト結果**

ドアのサーボがドアとともに前後に回転する。