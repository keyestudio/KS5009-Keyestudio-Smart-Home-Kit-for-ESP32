### 5.4.9 Proyecto 5.1 Controlar la puerta


#### **1. Descripción**

Las puertas y ventanas automáticas necesitan un dispositivo motorizado, que se volverá más automático con un servo de 180 grados y algunos sensores. Añadiendo un sensor de lluvia, se puede lograr el efecto de cerrar las ventanas automáticamente cuando llueve. Si se añade un RFID, podemos realizar el efecto de deslizar para abrir la puerta, etc.


#### **2. Conocimiento del componente**

**Servo:** Servo is a position servo driver device consists of a housing, a circuit board, a coreless motor, a gear and a position detector.

Su principio de funcionamiento es que el servo recibe la señal enviada por la MCU o el receptor y produce una señal de referencia con un periodo de 20ms y un ancho de 1,5ms, luego compara la tensión de polarización DC adquirida con la tensión del potenciómetro y obtiene la diferencia de tensión de salida.

El IC en la placa de circuito juzga la dirección de rotación, y luego impulsa el motor sin núcleo para que comience a girar. La potencia se transmite al brazo oscilante a través del engranaje reductor, y la señal se envía de vuelta por el detector de posición para juzgar si se ha alcanzado la posicionamiento, lo que es adecuado para aquellos sistemas de control que requieren un cambio de ángulo constante y que se pueden mantener.

Cuando la velocidad del motor es constante, el potenciómetro se hace girar a través del engranaje reductor en cascada, lo que hace que la diferencia de tensión sea 0, y el motor deja de girar. Generalmente, el rango de ángulo de rotación del servo es 0° --180 °.

El periodo de pulso del servo de control es 20ms, el ancho del pulso es 0.5ms ~ 2.5ms, y la posición correspondiente es -90°~ +90°. Aquí hay un ejemplo de un servo de 180°:

![imagen44](../media/708316fde05c62113a3024e0efb0c237.jpeg)

En general, el servo tiene tres cables en marrón, rojo y naranja. El cable marrón es masa, el rojo es el polo positivo y el naranja es la línea de señal.

![imagen45](../media/35084ae289a08e35bdb8c89ceb134ba4.png)

![imagen46](../media/6cbf6f177ea204f7632b872497fde010.png)


#### **3. Pines**

| El servo de la ventana | 5 |
| --- | --- |
| El servo de la puerta | 13 |


#### **4. Código de prueba**

```c
#include <ESP32Servo.h>
Servo myservo;  // create servo object to control a servo
                // 16 servo objects can be created on the ESP32

int pos = 0;    // variable to store the servo position
// Recommended PWM GPIO pins on the ESP32 include 2,4,12-19,21-23,25-27,32-33
int servoPin = 13;

void setup() {
    // Allow allocation of all timers
    ESP32PWM::allocateTimer(0);
    ESP32PWM::allocateTimer(1);
    ESP32PWM::allocateTimer(2);
    ESP32PWM::allocateTimer(3);
    myservo.setPeriodHertz(50);    // standard 50 hz servo
    myservo.attach(servoPin, 1000, 2000); // attaches the servo on pin 18 to the servo object
    // using default min/max of 1000us and 2000us
    // different servos may require different min/max settings
    // for an accurate 0 to 180 sweep

}

void loop() {
    for (pos = 0; pos <= 180; pos += 1) { // goes from 0 degrees to 180 degrees
        // in steps of 1 degree
        myservo.write(pos);    // tell servo to go to position in variable 'pos'
        delay(15);             // waits 15ms for the servo to reach the position
    }
    for (pos = 180; pos >= 0; pos -= 1) { // goes from 180 degrees to 0 degrees
        myservo.write(pos);    // tell servo to go to position in variable 'pos'
        delay(15);             // waits 15ms for the servo to reach the position
    }

}
```

#### **5. Resultado de la prueba**

El servo de la puerta se mueve con la puerta, de un lado a otro.