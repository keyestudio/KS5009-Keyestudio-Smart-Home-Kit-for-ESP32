### 5.4.22 Proyecto 13.1: Prueba de la APP para teléfono móvil


#### **1. Descargar APP**

**Android APP：**

**Método Uno：** El paquete de instalación apk de Android está disponible en
nuestro paquete de recursos, como se muestra a continuación:

![imagen64](../media/e1ad649f98cab75e4619b8fc1ca1e24a.png)

Puede transferir el apk de Android a su teléfono móvil mediante un cable USB y
luego instalarlo.

**Método Dos：** Descarga desde Google play:

Por favor busque **keyes IoT home** en Google play para descargarla.

![imagen65](../media/img-20250619170100.png)

**Interfaz de la APP**

![imagen66](../media/8e7c339852876017b41a39d5a0b31323.png)

**Descargar la APP iOS**

Por favor busque **keyes IoT home** en App Store para descargarla.

![imagen67](../media/img-20250619170100.png)

**Interfaz de la APP**

![imagen68](../media/8e7c339852876017b41a39d5a0b31323.png)


#### **2. Descripción**

Usaremos la APP para controlar las luces LED del hogar inteligente y los interruptores del ventilador.


#### **3. Código de prueba**

⚠️ \ **ATENCIÓN:**\  Después de abrir el archivo de código, debe modificar
el nombre y la contraseña de WiFi que la placa de desarrollo ESP32 necesita
para conectarse. Reemplace `ChinaNet-2.4G-0DF0` y `ChinaNet@233` por
su propio nombre y contraseña de WiFi respectivamente. Debe hacer esto antes
de subir el código; de lo contrario, la placa ESP32 no podrá
conectarse a la red.

```c
const char* ssid = "ChinaNet-2.4G-0DF0";  // Enter your own WiFi name
const char* pwd = "ChinaNet@233"; // Enter your own WiFi passwords
```
⚠️ **NOTA: Por favor asegúrese de que el nombre y la contraseña de WiFi en el código
sean los mismos que la red a la que están conectados su ordenador, teléfono
móvil/tableta, la placa de desarrollo ESP32 y el router. Deben estar dentro
de la misma red de área local (WiFi).**

⚠️ **NOTA: El WiFi debe estar en una frecuencia de 2.4Ghz; de lo contrario, el ESP32
no puede conectarse al WiFi.**

```c
#include <Arduino.h>
#ifdef ESP32
#include <WiFi.h>
#elif defined(ESP8266)
#include <ESP8266WiFi.h>
#endif

#include <LiquidCrystal_I2C.h>

#define fanPin1 19 //IN+ pin
#define fanPin2 18 //IN- pin
#define led_y 12  //Define the yellow led pin as 12

const char* ssid = "ChinaNet-2.4G-0DF0";
const char* pwd = "ChinaNet@233";

#include <Wire.h>
//Initialize the LCD address, columns and rows
LiquidCrystal_I2C lcd(0x27, 16, 2);

WiFiServer server(80);  //Initialize the WiFi service

//Define the variable as the detected value
String request;

unsigned long prevTask = 0;

void setup() {
  Serial.begin(9600);
  //Connect to wifi
  WiFi.begin(ssid, pwd);
  //Determine whether it is connected
  Serial.println("Connecting to WiFi...");
  while (WiFi.status() != WL_CONNECTED) {
    delay(1000);
    Serial.print(".");
  }
  delay(1000);
  //The serial monitor will display the name and IP address of the wireless network
  Serial.println("Connected to WiFi");
  Serial.print("WiFi NAME:");
  Serial.println(ssid);
  Serial.print("IP:");
  Serial.println(WiFi.localIP());

  //Initialize the LCD
  lcd.init();
  //Turn on the LCD backlight
  lcd.backlight();
  //lcd.noBacklight();
  lcd.clear();
  //Set the position of the cursor
  lcd.setCursor(0, 0);
  //LCD printing
  lcd.print("IP:");
  //Set the position of the cursor
  lcd.setCursor(0, 1);
  //LCD printing
  lcd.print(WiFi.localIP());

  //Set pin modes
  pinMode(led_y, OUTPUT);
  pinMode(fanPin1, OUTPUT);
  pinMode(fanPin2, OUTPUT);
  //Start the service
  server.begin();
}

void loop() {
  //Check whether the client has been connected to the network server
  //When the client establishes a connection with the server, the "server.available()" function returns a WiFiClient object for client-side communication.
  WiFiClient client = server.available();
  if (client) {
    Serial.println("New client connected");
    while (client.connected()) {
      //Determine whether the server sends data
      if (client.available()) {
        request = client.readStringUntil('s');
        Serial.print("Received message: ");
        Serial.println(request);
      }

      //LED
      if (request == "a") {
        digitalWrite(led_y, HIGH);
      } else if (request == "A") {
        digitalWrite(led_y, LOW);
      }

      //fan
      if (request == "f") {
        digitalWrite(fanPin1, LOW); //pwm = 0
        analogWrite(fanPin2, 100); //LEDC channel 5 is bound to the specified left motor output PWM value as 100.
      } else if (request == "F") {
        digitalWrite(fanPin1, LOW); //pwm = 0
        analogWrite(fanPin2, 0); //LEDC channel 5 is bound to the specified left motor output PWM value as 0.
      }

      request = "";
    }
    Serial.println("Client disconnected");
  }
}
```

#### **4. Resultado de la prueba**

⚠️ **NOTA: El teléfono móvil o la tableta deben estar conectados a la placa de desarrollo ESP32 a través del mismo WiFi. De lo contrario, no podrá acceder a la página de control. Además, cuando la placa de desarrollo ESP32 usa la función WiFi, consume mucha energía. Se requiere una fuente de alimentación DC externa para satisfacer su demanda de potencia para su funcionamiento. Si la demanda de energía no se satisface, la placa ESP32 se reiniciará continuamente, lo que provocará que el código no se ejecute con normalidad.**

1. Abra la APP y seleccione WiFi

![imagen69](../media/ac7304f39a53b2318825db72e5085753.png)

1. La APP controla el LED y el ventilador

El teléfono móvil y el hogar inteligente deben compartir el mismo WiFi, o el hogar inteligente se conecta al hotspot del teléfono móvil.

La APP introduce la dirección IP (LCD1602 muestra la dirección IP asignada), luego haga clic en conectar; la conexión es exitosa si se muestra la IP del ESP32: 192.168.xx.xx.

A continuación, puede pulsar el LED, entonces el LED del hogar inteligente se encenderá. Pulse el botón del ventilador y el ventilador se encenderá, como se muestra a continuación:

![imagen70](../media/aba40215ce81fc7c326f6666c67059b8.png)