### 5.4.22 Project 13.1: Mobiele telefoon APP-test


#### **1. APP downloaden**

**Android APP：**

**Methode één：** Het Android apk-installatiepakket is beschikbaar in ons resourcepakket, zoals hieronder weergegeven:

![afbeelding64](../media/e1ad649f98cab75e4619b8fc1ca1e24a.png)

Je kunt de Android apk via een USB-kabel naar je mobiele telefoon overbrengen en vervolgens installeren.

**Methode twee：** Download van Google play:

Zoek op Google Play naar **keyes IoT home** om het te downloaden.

![afbeelding65](../media/img-20250619170100.png)

**APP-interface**

![afbeelding66](../media/8e7c339852876017b41a39d5a0b31323.png)

**iOS APP downloaden**

Zoek in de App Store naar **keyes IoT home** om het te downloaden.

![afbeelding67](../media/img-20250619170100.png)

**APP-interface**

![afbeelding68](../media/8e7c339852876017b41a39d5a0b31323.png)


#### **2. Beschrijving**

We gebruiken de APP om de smart home LED-lampen en ventilatorschakelaars te bedienen.


#### **3. Testcode**

⚠️ **WAARSCHUWING:** Nadat je het codebestand hebt geopend, moet je de WiFi-naam en wachtwoorden wijzigen waarmee het ESP32 development board verbinding moet maken. Vervang `ChinaNet-2.4G-0DF0` en `ChinaNet@233` respectievelijk door je eigen WiFi-naam en wachtwoord. Je moet dit doen voordat je de code uploadt; anders kan het ESP32 board niet met het netwerk verbinden.

```c
const char* ssid = "ChinaNet-2.4G-0DF0";  // Enter your own WiFi name
const char* pwd = "ChinaNet@233"; // Enter your own WiFi passwords
```
⚠️ **OPMERKING: Zorg ervoor dat de WiFi-naam en wachtwoorden in de code hetzelfde zijn als het netwerk waarmee je computer, mobiele telefoon/tablet, ESP32 development board en router zijn verbonden. Ze moeten zich binnen hetzelfde lokale netwerk (WiFi) bevinden.**

⚠️ **OPMERKING: De WiFi moet op een 2.4Ghz-frequentie zitten; anders kan de ESP32 geen verbinding maken met WiFi.**

```c
#include <Arduino.h>
#ifdef ESP32
#include <WiFi.h>
#elif defined(ESP8266)
#include <ESP8266WiFi.h>
#endif

#include <LiquidCrystal_I2C.h>

#define fanPin1 19 //IN+ pin
#define fanPin2 18 //IN- pin
#define led_y 12  //Define the yellow led pin as 12

const char* ssid = "ChinaNet-2.4G-0DF0";
const char* pwd = "ChinaNet@233";

#include <Wire.h>
//Initialize the LCD address, columns and rows
LiquidCrystal_I2C lcd(0x27, 16, 2);

WiFiServer server(80);  //Initialize the WiFi service

//Define the variable as the detected value
String request;

unsigned long prevTask = 0;

void setup() {
  Serial.begin(9600);
  //Connect to wifi
  WiFi.begin(ssid, pwd);
  //Determine whether it is connected
  Serial.println("Connecting to WiFi...");
  while (WiFi.status() != WL_CONNECTED) {
    delay(1000);
    Serial.print(".");
  }
  delay(1000);
  //The serial monitor will display the name and IP address of the wireless network
  Serial.println("Connected to WiFi");
  Serial.print("WiFi NAME:");
  Serial.println(ssid);
  Serial.print("IP:");
  Serial.println(WiFi.localIP());

  //Initialize the LCD
  lcd.init();
  //Turn on the LCD backlight
  lcd.backlight();
  //lcd.noBacklight();
  lcd.clear();
  //Set the position of the cursor
  lcd.setCursor(0, 0);
  //LCD printing
  lcd.print("IP:");
  //Set the position of the cursor
  lcd.setCursor(0, 1);
  //LCD printing
  lcd.print(WiFi.localIP());

  //Set pin modes
  pinMode(led_y, OUTPUT);
  pinMode(fanPin1, OUTPUT);
  pinMode(fanPin2, OUTPUT);
  //Start the service
  server.begin();
}

void loop() {
  //Check whether the client has been connected to the network server
  //When the client establishes a connection with the server, the "server.available()" function returns a WiFiClient object for client-side communication.
  WiFiClient client = server.available();
  if (client) {
    Serial.println("New client connected");
    while (client.connected()) {
      //Determine whether the server sends data
      if (client.available()) {
        request = client.readStringUntil('s');
        Serial.print("Received message: ");
        Serial.println(request);
      }

      //LED
      if (request == "a") {
        digitalWrite(led_y, HIGH);
      } else if (request == "A") {
        digitalWrite(led_y, LOW);
      }

      //fan
      if (request == "f") {
        digitalWrite(fanPin1, LOW); //pwm = 0
        analogWrite(fanPin2, 100); //LEDC channel 5 is bound to the specified left motor output PWM value as 100.
      } else if (request == "F") {
        digitalWrite(fanPin1, LOW); //pwm = 0
        analogWrite(fanPin2, 0); //LEDC channel 5 is bound to the specified left motor output PWM value as 0.
      }

      request = "";
    }
    Serial.println("Client disconnected");
  }
}
```

#### **4. Testresultaat**

⚠️ **Opmerking: De mobiele telefoon of tablet moet via dezelfde WiFi met het ESP32 development board verbonden zijn. Anders kan deze de bedieningspagina niet bereiken. Bovendien verbruikt het ESP32 development board, wanneer het WiFi gebruikt, veel stroom. Een externe DC-voeding is vereist om aan deze stroombehoefte te voldoen. Als de voeding onvoldoende is, blijft het ESP32 board resetten, waardoor de code niet normaal draait.**

1. Open de APP en selecteer WiFi

![afbeelding69](../media/ac7304f39a53b2318825db72e5085753.png)

1. De APP bedient de LED en de ventilator

De mobiele telefoon en het smart home moeten hetzelfde WiFi-netwerk gebruiken, of het smart home moet verbinding maken met de hotspot van de mobiele telefoon.

Voer in de APP het IP-adres in (LCD1602 toont het toegewezen IP-adres), klik vervolgens op verbinden; de verbinding is succesvol als het ESP32 IP: 192.168.xx.xx wordt weergegeven.

Vervolgens kun je op de LED klikken, waarna de smart home LED wordt ingeschakeld. Klik op de ventilatorknop en de ventilator wordt ingeschakeld, zoals hieronder weergegeven:

![afbeelding70](../media/aba40215ce81fc7c326f6666c67059b8.png)