### 5.4.22 Projet 13.1 : Test de l'application mobile


#### **1. Télécharger l'APP**

**APP Android：**

**Méthode 1：** Le paquet d'installation apk Android est disponible dans notre pack de ressources, comme indiqué ci‑dessous :

![image64](../media/e1ad649f98cab75e4619b8fc1ca1e24a.png)

Vous pouvez transférer l'apk Android sur votre téléphone via un câble USB, puis l'installer.

**Méthode 2：** Télécharger depuis Google Play :

Veuillez rechercher **keyes IoT home** sur Google Play pour le télécharger.

![image65](../media/img-20250619170100.png)

**Interface de l'application**

![image66](../media/8e7c339852876017b41a39d5a0b31323.png)

**Télécharger l'APP iOS**

Veuillez rechercher **keyes IoT home** sur l'App Store pour le télécharger.

![image67](../media/img-20250619170100.png)

**Interface de l'application**

![image68](../media/8e7c339852876017b41a39d5a0b31323.png)


#### **2. Description**

Nous utiliserons l'APP pour contrôler les lumières LED et les interrupteurs du ventilateur de la maison intelligente.


#### **3. Code de test**

⚠️ **ATTENTION :** Après avoir ouvert le fichier de code, vous devez modifier le nom et le mot de passe WiFi auxquels la carte de développement ESP32 doit se connecter. Remplacez `ChinaNet-2.4G-0DF0` et `ChinaNet@233` par votre propre nom et mot de passe WiFi respectivement. Vous devez le faire avant de téléverser le code ; sinon, la carte ESP32 ne pourra pas se connecter au réseau.

```c
const char* ssid = "ChinaNet-2.4G-0DF0";  // Enter your own WiFi name
const char* pwd = "ChinaNet@233"; // Enter your own WiFi passwords
```
⚠️ **REMARQUE : Veuillez vous assurer que le nom et le mot de passe WiFi dans le code sont les mêmes que ceux du réseau auquel sont connectés votre ordinateur, votre téléphone/tablette, la carte de développement ESP32 et le routeur. Ils doivent être sur le même réseau local (WiFi).**

⚠️ **REMARQUE : Le WiFi doit être sur une fréquence 2.4 GHz ; sinon, l'ESP32 ne pourra pas se connecter au WiFi.**

```c
#include <Arduino.h>
#ifdef ESP32
#include <WiFi.h>
#elif defined(ESP8266)
#include <ESP8266WiFi.h>
#endif

#include <LiquidCrystal_I2C.h>

#define fanPin1 19 //IN+ pin
#define fanPin2 18 //IN- pin
#define led_y 12  //Define the yellow led pin as 12

const char* ssid = "ChinaNet-2.4G-0DF0";
const char* pwd = "ChinaNet@233";

#include <Wire.h>
//Initialize the LCD address, columns and rows
LiquidCrystal_I2C lcd(0x27, 16, 2);

WiFiServer server(80);  //Initialize the WiFi service

//Define the variable as the detected value
String request;

unsigned long prevTask = 0;

void setup() {
  Serial.begin(9600);
  //Connect to wifi
  WiFi.begin(ssid, pwd);
  //Determine whether it is connected
  Serial.println("Connecting to WiFi...");
  while (WiFi.status() != WL_CONNECTED) {
    delay(1000);
    Serial.print(".");
  }
  delay(1000);
  //The serial monitor will display the name and IP address of the wireless network
  Serial.println("Connected to WiFi");
  Serial.print("WiFi NAME:");
  Serial.println(ssid);
  Serial.print("IP:");
  Serial.println(WiFi.localIP());

  //Initialize the LCD
  lcd.init();
  //Turn on the LCD backlight
  lcd.backlight();
  //lcd.noBacklight();
  lcd.clear();
  //Set the position of the cursor
  lcd.setCursor(0, 0);
  //LCD printing
  lcd.print("IP:");
  //Set the position of the cursor
  lcd.setCursor(0, 1);
  //LCD printing
  lcd.print(WiFi.localIP());

  //Set pin modes
  pinMode(led_y, OUTPUT);
  pinMode(fanPin1, OUTPUT);
  pinMode(fanPin2, OUTPUT);
  //Start the service
  server.begin();
}

void loop() {
  //Check whether the client has been connected to the network server
  //When the client establishes a connection with the server, the "server.available()" function returns a WiFiClient object for client-side communication.
  WiFiClient client = server.available();
  if (client) {
    Serial.println("New client connected");
    while (client.connected()) {
      //Determine whether the server sends data
      if (client.available()) {
        request = client.readStringUntil('s');
        Serial.print("Received message: ");
        Serial.println(request);
      }

      //LED
      if (request == "a") {
        digitalWrite(led_y, HIGH);
      } else if (request == "A") {
        digitalWrite(led_y, LOW);
      }

      //fan
      if (request == "f") {
        digitalWrite(fanPin1, LOW); //pwm = 0
        analogWrite(fanPin2, 100); //LEDC channel 5 is bound to the specified left motor output PWM value as 100.
      } else if (request == "F") {
        digitalWrite(fanPin1, LOW); //pwm = 0
        analogWrite(fanPin2, 0); //LEDC channel 5 is bound to the specified left motor output PWM value as 0.
      }

      request = "";
    }
    Serial.println("Client disconnected");
  }
}
```

#### **4. Résultat du test**

⚠️ **Remarque : Le téléphone mobile ou la tablette doit être connecté(e) à la carte de développement ESP32 via le même WiFi. Sinon, il ne pourra pas accéder à la page de contrôle. De plus, lorsque la carte ESP32 utilise la fonction WiFi, elle consomme beaucoup d'énergie. Une alimentation DC externe est requise pour satisfaire sa demande en énergie pendant le fonctionnement. Si l'alimentation est insuffisante, la carte ESP32 se réinitialisera en continu, ce qui empêchera le code de s'exécuter normalement.**

1. Ouvrez l'APP et sélectionnez WiFi

![image69](../media/ac7304f39a53b2318825db72e5085753.png)

1. L'APP contrôle la LED et le ventilateur

Le téléphone mobile et la maison intelligente doivent partager le même WiFi, ou la maison intelligente peut se connecter au hotspot du mobile.

Dans l'APP, saisissez l'adresse IP (LCD1602 affiche l'adresse IP attribuée), puis cliquez sur connecter ; la connexion est réussie si l'adresse IP de l'ESP32 s'affiche : 192.168.xx.xx.

Ensuite, vous pouvez cliquer sur la LED pour allumer la LED de la maison intelligente. Cliquez sur le bouton du ventilateur pour l'allumer, comme montré ci‑dessous :

![image70](../media/aba40215ce81fc7c326f6666c67059b8.png)