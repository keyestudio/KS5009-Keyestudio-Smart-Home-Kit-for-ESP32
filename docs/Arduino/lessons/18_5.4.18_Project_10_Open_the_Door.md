### 5.4.18 Proyecto 10 Abrir la puerta


#### **1. Conocimiento de componentes**

Identificación por radiofrecuencia, el lector de tarjetas está compuesto por un módulo de radiofrecuencia y un campo magnético de alta intensidad. El transpondedor Tag es un dispositivo de detección que no contiene batería. Solo contiene pequeños chips de circuito integrado, medios para almacenar datos y antenas para recibir y transmitir señales.

Para leer los datos de la etiqueta, primero colócala dentro del rango de lectura del lector de tarjetas. El lector generará un campo magnético, que según la ley de Lenz puede producir electricidad; entonces la etiqueta RFID suministrará energía, activando así el dispositivo.

![imagen53](../media/982ac6a9da0e8f55465ca5a969ac0dfe.png)


#### **2. Pines de control**

Usar comunicación IIC

| SDA | SDA |
| --- | --- |
| SCL | SCL |


#### **3. Código de prueba**

```c
//**********************************************************************************
/*
 * Filename    : RFID
 * Description : RFID reader UID
 * Auther      : http//www.keyestudio.com
*/
#include <Wire.h>
#include <LiquidCrystal_I2C.h>
LiquidCrystal_I2C mylcd(0x27,16,2);
#include <ESP32Servo.h>
Servo myservo;
#include <Wire.h>
#include "MFRC522_I2C.h"
// IIC pins default to GPIO21 and GPIO22 of ESP32
// 0x28 is the i2c address of SDA, if doesn't match，please check your address with i2c.
MFRC522 mfrc522(0x28);   // create MFRC522.
#define servoPin  13
#define btnPin 16
boolean btnFlag = 0;

String password = "";

void setup() {
  Serial.begin(115200);           // initialize and PC's serial communication
  mylcd.init();
  mylcd.backlight();
  Wire.begin();                   // initialize I2C
  mfrc522.PCD_Init();             // initialize MFRC522
  ShowReaderDetails();            // dispaly PCD - MFRC522 read carder
  Serial.println(F("Scan PICC to see UID, type, and data blocks..."));

    // Allow allocation of all timers
    ESP32PWM::allocateTimer(0);
    ESP32PWM::allocateTimer(1);
    ESP32PWM::allocateTimer(2);
    ESP32PWM::allocateTimer(3);
    myservo.setPeriodHertz(50);    // standard 50 hz servo
    myservo.attach(servoPin, 1000, 2000); // attaches the servo on pin 18 to the servo object
    // using default min/max of 1000us and 2000us
    // different servos may require different min/max settings
    // for an accurate 0 to 180 sweep

  mylcd.setCursor(0, 0);
  mylcd.print("Card");
}

void loop() {
  //
  if ( ! mfrc522.PICC_IsNewCardPresent() || ! mfrc522.PICC_ReadCardSerial() ) {
    delay(50);
    password = "";
    if(btnFlag == 1)
    {
      boolean btnVal = digitalRead(btnPin);
      if(btnVal == 0)  //If door close button is pressed (active-low)
      {
        Serial.println("close");
        mylcd.setCursor(0, 0);
        mylcd.print("close");
        myservo.write(0);
        btnFlag = 0;
      }
    }
    return;
  }

  // select one of door cards. UID and SAK are mfrc522.uid.

  // save UID
  Serial.print(F("Card UID:"));
  for (byte i = 0; i < mfrc522.uid.size; i++) {
    Serial.print(mfrc522.uid.uidByte[i] < 0x10 ? " 0" : " ");
    //Serial.print(mfrc522.uid.uidByte[i], HEX);
    Serial.print(mfrc522.uid.uidByte[i]);
    password = password + String(mfrc522.uid.uidByte[i]);
  }
  if(password == "")  //Card number is correct,open the door
  {
    Serial.println("open");
    mylcd.setCursor(0, 0);
    mylcd.clear();
    mylcd.print("open");
    myservo.write(180);
    password = "";
    btnFlag = 1;
  }
  else   //Card number error,dispaly error
  {
    password = "";
    mylcd.setCursor(0, 0);
    mylcd.print("error");
  }
  //Serial.println(password);
}

void ShowReaderDetails() {
  //  attain the MFRC522 software
  byte v = mfrc522.PCD_ReadRegister(mfrc522.VersionReg);
  Serial.print(F("MFRC522 Software Version: 0x"));
  Serial.print(v, HEX);
  if (v == 0x91)
    Serial.print(F(" = v1.0"));
  else if (v == 0x92)
    Serial.print(F(" = v2.0"));
  else
    Serial.print(F(" (unknown)"));
  Serial.println("");
  // when returning to 0x00 or 0xFF, may fail to transmit communication signals
  if ((v == 0x00) || (v == 0xFF)) {
    Serial.println(F("WARNING: Communication failure, is the MFRC522 properly connected?"));
  }
}
//**********************************************************************************
```

#### **4. Resultado de la prueba**

Sube el código, en la LCD1602 se mostrará "Card", abre el monitor serie y ajusta la velocidad de baudios a "115200".

Acerca la tarjeta proporcionada al área de inducción RFID; en la LCD1602 se mostrará "error", pero la salida del monitor serie es la que se muestra en la figura:

![imagen54](../media/bb.png)

Introduce el "Card UID" de la imagen en la posición que se muestra en la figura (elimina los espacios en el "Card UID" y en el **Card UID** del monitor serie; elimina el **0** inicial solo si aparece **antes de cualquier dígito** (p. ej., `" 0123"` → `"123"`), pero conserva el **0** si sigue a un número (p. ej., `"601"` permanece `"601"`)):

![imagen55](../media/cc.png)

Sube el código, acerca la tarjeta proporcionada al área de inducción RFID, la puerta girará y se abrirá, y la LCD1602 mostrará "open".

Pulsa el botón 1 y la puerta gira y se cierra. Sin embargo, al pasar otra tarjeta/etiqueta inductiva azul, la LCD1602 muestra "Error".